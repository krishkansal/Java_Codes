package Sorting;

public class Merge_Sort {
	
	private static void merge(int[] ar, int[] arr, int low, int high, int mid) {
		for(int k=0;k<ar.length;k++) {
			arr[k]=ar[k];
			int i=low;
			j=mid+1;
			for(int k=low;k<=high;k++) {
				if(i>)
					
			}
		}
	
	}
	
	public static void main(String[] args) {

		int [] ar= {5,11,17,23,27,9,13,15,35,45};
		int [] arr=new int[ar.length];
		int low=0;
		int high=arr.length-1;
		int mid=(low+high)/2;
		merge(ar,arr,low,high,mid);
			
	}

}
